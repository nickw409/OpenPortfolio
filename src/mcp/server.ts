// Phase 12 — MCP server entry point. Read-only enforcement happens at the
// SQLite connection layer (open in read-only mode), not at the query layer.
console.log('mcp server stub — see docs/WORKSTREAMS.md Phase 12');
